docker run -d --name lair -p 8080:80 --link spawning-pool wordpress
#Phpmyadmin must point to MySQL Server. So that we must link both containers by adding the option : --link name-of-container:name-of-imag. :name-of-img is not obligatoire
#docker run --name myadmin -d --link mysql:db -p 8080:80 phpmyadmin/phpmyadmin`
